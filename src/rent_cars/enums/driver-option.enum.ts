// src/cars_lite/enums/driver-option.enum.ts
export enum DriverOption {
  WITH_DRIVER = 'with_driver',      // с водителем
  WITHOUT_DRIVER = 'without_driver',// без водителя
  BOTH = 'both',                    // и так, и так
}
